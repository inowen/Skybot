package inowen.gui.screens.configTabs;

import inowen.gui.screens.MainConfigScreen;
import net.minecraft.util.text.ITextComponent;

public class MelonPumpkinBotConfigScreen extends MainConfigScreen {

    /**
     * Constructor.
     *
     * @param titleIn Title for the window (can be accessed through this.title)
     */
    public MelonPumpkinBotConfigScreen(ITextComponent titleIn) {
        super(titleIn);
    }

    @Override
    public void init() {
        super.init();
    }

    @Override
    public void render(int mouseX, int mouseY, float partialTicks) {
        super.render(mouseX, mouseY, partialTicks);
    }

    @Override
    public void tick() {
        super.tick();
    }
}
